import { ComingSoon } from './_shell';

export function ResolutionReport() {
  return (
    <ComingSoon
      title="Resolution times"
      description="Mean time to resolve (MTTR) by priority, type and team."
    />
  );
}
