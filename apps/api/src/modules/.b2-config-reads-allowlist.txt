# B.2 raw config-reads allowlist
# ------------------------------
# Each line is the EXACT ripgrep output (file:content), with line numbers
# stripped and the result sorted+deduped. Generated by
#   scripts/check-b2-config-reads.sh --gen
#
# Why this exists: docs/follow-ups/b2-survey-and-design.md §0.2 + §0.3.
# The B.2 contract says the runtime row carries the effective workflow /
# SLA plan after create/grant/reclassify. Raw reads of
#   workflow_definition_id, sla_policy_id, case_sla_policy_id
# in B.2-scope modules are limited to: resolver/plan-build, reclassify
# resolution, runtime column accesses on sla_timers, workflow node config
# payload, doc comments. Anything else is a failure-class regression.
#
# Lifecycle:
#   - "legit": permanent. Documented as a legitimate read in §0.2.
#   - "to-fix": temporary. Will be eliminated when the named B.2.A RPC
#     ships (§3.5 / §3.10 / §3.11). Tagged inline.
#   - "pending review": new entry that hasn't been triaged. CI fails on
#     any pending-review entry.
#
# This is a SNAPSHOT, not approval-forever. Re-triage on every PR that
# touches a B.2-scope module.
#
# Format: each non-comment line is a content key, sorted.

apps/api/src/modules/sla/sla.service.ts:        const thresholds = thresholdsByPolicy.get(`${timer.sla_policy_id}|${timer.tenant_id}`) ?? [];
apps/api/src/modules/sla/sla.service.ts:        sla_policy_id: slaPolicyId,
apps/api/src/modules/sla/sla.service.ts:      .select('id, tenant_id, ticket_id, sla_policy_id, timer_type, target_minutes, started_at, due_at, total_paused_minutes')
apps/api/src/modules/sla/sla.service.ts:      set.add(t.sla_policy_id);
apps/api/src/modules/sla/sla.service.ts:    // bypasses RLS, so a smuggled cross-tenant sla_policy_id on a timer would
apps/api/src/modules/sla/sla.service.ts:    const policyName = await this.loadPolicyName(timer.sla_policy_id, timer.tenant_id);
apps/api/src/modules/ticket/dispatch.service.ts:   * Resolve which sla_policy_id to attach to a child work order.
apps/api/src/modules/ticket/dispatch.service.ts: * `workflow_definition_id`, `sla_policy_id`.
apps/api/src/modules/ticket/reclassify.service.ts:        ? this.loadWorkflowDefinition(newType.workflow_definition_id, tenant.id)
apps/api/src/modules/ticket/reclassify.service.ts:        effectiveOverride?.case_sla_policy_id ?? newType.sla_policy_id ?? null,
apps/api/src/modules/ticket/reclassify.service.ts:        effectiveOverride?.workflow_definition_id ?? newType.workflow_definition_id ?? null,
apps/api/src/modules/ticket/reclassify.service.ts:      .select('id, name, domain, active, sla_policy_id, workflow_definition_id')
apps/api/src/modules/ticket/reclassify.service.ts:      newType.sla_policy_id ? this.loadSlaPolicy(newType.sla_policy_id, tenant.id) : Promise.resolve(null),
apps/api/src/modules/ticket/reclassify.service.ts:      newType.workflow_definition_id
apps/api/src/modules/ticket/ticket.service.ts:              'id, domain, sla_policy_id, workflow_definition_id, requires_approval, approval_approver_team_id, approval_approver_person_id',
apps/api/src/modules/ticket/ticket.service.ts:      (requestTypeCfg?.sla_policy_id as string | null | undefined) ??
apps/api/src/modules/ticket/ticket.service.ts:      (requestTypeCfg?.workflow_definition_id as string | null | undefined) ??
apps/api/src/modules/ticket/ticket.service.ts:      (scopeOverride?.case_sla_policy_id as string | null | undefined) ??
apps/api/src/modules/ticket/ticket.service.ts:      (scopeOverride?.workflow_definition_id as string | null | undefined) ??
apps/api/src/modules/ticket/ticket.service.ts:      // workflow_definition_id and emits workflow.start_required with
apps/api/src/modules/ticket/ticket.service.ts:      sla_id: args.sla_policy_id ?? null,
apps/api/src/modules/ticket/ticket.service.ts:   * Pre-empts PG's semantic re-derivation for workflow_definition_id.
apps/api/src/modules/ticket/ticket.service.ts: * `workflow_definition_id`, `sla_policy_id`.
apps/api/src/modules/workflow/workflow-engine.service.ts:              ? { sla_id: task.sla_policy_id ?? null }
apps/api/src/modules/workflow/workflow-engine.service.ts:            ...(Object.prototype.hasOwnProperty.call(task, 'sla_policy_id')
apps/api/src/modules/workflow/workflow-engine.service.ts:            // sla_policy_id semantics preserved: explicit key (including
apps/api/src/modules/workflow/workflow-engine.service.ts:        workflow_definition_id: workflowDefinitionId,
apps/api/src/modules/workflow/workflow-engine.service.ts:      .eq('id', instance.workflow_definition_id)
apps/api/src/modules/workflow/workflow-engine.service.ts:      .select('id, workflow_definition_id, current_node_id, ticket_id')
apps/api/src/modules/workflow/workflow-engine.service.ts:    // foreign workflow_definition_id (FK-smuggle) would load a foreign
